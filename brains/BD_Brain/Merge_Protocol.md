# Merge Protocol

1. Validate candidate records against `Schemas/brain_core.schema.json`.
2. Check for duplicate IDs and semantic overlap.
3. Route disputes to `Conflict_Log.md`.
4. Append accepted records to `BD_Brain_Core.jsonl`.
5. Update `Assertions_Index.md`.
