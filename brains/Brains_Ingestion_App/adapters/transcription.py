from __future__ import annotations


def transcribe_source(source: dict) -> str:
    return (
        f"[DUMMY TRANSCRIPT] Source {source['source_id']} ({source['title']}). "
        "Replace with real transcription provider integration."
    )
